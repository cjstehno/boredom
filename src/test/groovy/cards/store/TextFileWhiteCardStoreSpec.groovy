package cards.store

import spock.lang.Specification

class TextFileWhiteCardStoreSpec extends Specification {

    // FIXME: consider loading the files, shuffling them and then drawing like a real deck, rather than random pick

    def 'white cards'(){
        when:
        def x = 1

        then:
        x
    }
}
